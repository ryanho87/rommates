[project]
name = "rommates"
version = "0.1.0"
description = "Private ROM library cleanup and device deployment manager"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.141.1",
  "mcp==2.1.1",
  "uvicorn[standard]==0.52.4",
]

[project.optional-dependencies]
dev = [
  "httpx==0.28.1",
  "pytest==8.4.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
