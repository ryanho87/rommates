Device information now agrees with the ROMmates web app and is easier to scan.

• Device pages refresh against the NUC filesystem instead of reusing stale cached inventory.
• Storage clearly separates bytes currently used, the selected roster, and staged changes.
• See on-device ROM counts and storage totals for every platform.
• Download a device’s complete selected roster as one ES-DE-ready ZIP and share it from iOS.
• Platform and device-state colors make long game lists faster to read without relying on color alone.
