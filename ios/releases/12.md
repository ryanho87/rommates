Device pages now load without a false “Not found” alert when fetching storage details.

• The public mobile API now exposes the device summary used by the native app.
• Storage details gracefully fall back to the device inventory if that optional request is unavailable.
• Rows no longer repeat an “On device” badge—the toggle already communicates that state.
• Pending additions, updates, and removals remain labeled so staged changes stay clear.
