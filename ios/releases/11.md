Syncthing setup can now be completed entirely from the iOS app.

• Enter a handheld’s Syncthing Device ID from its device page.
• ROMmates validates the ID, creates or reuses the ROM folder, shares it, and requests a scan.
• Clear setup guidance explains where to find the ID and what will change on the NUC.
• Setup errors remain retryable and successful shares show the resulting folder details.
