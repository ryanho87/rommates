Syncthing setup has a cleaner call to action.

• The setup button now centers its text independently instead of centering an icon-and-text label as one unit.
• The button keeps a consistent native tap target without invisible icon spacing.
